{
  "name": "korver.dev",
  "author": "Casey Korver",
  "email": "casey@korver.dev",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "build": "npx @11ty/eleventy --input=src/ --output=build/",
    "start": "npx @11ty/eleventy --input=src/ --output=build/ --serve --incremental"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
